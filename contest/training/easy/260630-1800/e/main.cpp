#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (n); ++i)
#define ll long long

int main() {
    int n;
    cin >> n;

    if (n < 4) {
        cout << n << endl;
        return 0;
    }

    vector<int> list(n);

    rep (i, n) {
        cin >> list[i];
    }

    vector<int> remove;

    


    return 0;
}

// --------------------------------
// Reference
// --------------------------------
