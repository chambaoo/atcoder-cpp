#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (n); ++i)
#define ll long long

int main() {
    int q;
    cin >> q;

    vector<ll> v;

    vector<ll> sum;
    ll deleted = 0;

    ll current = 0;

    rep (i, q) {
        int type;
        cin >> type;

        if (type == 1) {
            ll c, x;
            


        } else {
            ll k;

        }

    }

    return 0;
}